# SKILL 버전 이력 (Changelog)

이 파일은 `SKILL_v*.md`의 전체 버전 이력을 보관하는 append-only 전역 문서다. SKILL.md 본문에는 이 이력을 싣지 않는다 — 스킬 로드 시 컨텍스트를 소모하지 않도록 분리했으며, 과거 버전의 상세 배경이 필요할 때만 이 파일을 읽는다. 각 버전의 원문 전체는 `Archive/Archive_Skill/`의 구버전 파일에 그대로 남아 있다.

운영 규칙: SKILL.md가 새 버전으로 갱신될 때마다 이 파일 하단 표에 해당 버전의 항목을 추가한다(기존 항목은 수정하지 않는다). 이 파일 자체는 버전 접미사 없이 제자리에서 추가만 하는 예외 문서다(CLAUDE.md와 동일한 취급).

| Version | 변경 내용 |
|---|---|
| v1.0 | 초안 — 핵심 파싱 원칙(스키마 동적 발견, 데이터 정제, 측정 기준 분리, 무결성 유지), 마크다운+XML 하이브리드 문서 구조 채택 |
| v1.1 | 출력 포맷 규칙 4종 추가: 섹션 단위 출처 표기, 표 순수성 유지, 데이터 충돌 보존, L-Acoustics 브랜드 OM 우선 출처 규칙 |
| v1.2 | 이모지/특수 아이콘 사용 전면 금지 규칙 추가 |
| v1.3 | 데이터 정제 규칙에 저항값 유럽식 표기(2R7 등) 표준화 추가(Value와 Key 양쪽 모두 적용), 무결성 원칙에 "원본에 없는 수식어·마케팅 문구·괄호 설명 임의 첨가 절대 금지" 조항 강화 |
| v1.4 | Phase 3을 3-1(동종 브랜드 이기종 채널)/3-2(타 브랜드 교차)로 세분화하고, 신규 속성 발견 시 기존 제품 파일 전체에 `null` 소급 반영 및 갱신 파일 일괄 재출력을 필수 요건으로 명문화. Phase 4에 누적 제품 5개 이상일 때의 출력 방식 예외 규정 추가 |
| v1.5 | "PDF 문서 분석 및 이미지 처리 규칙" 절 신설 — PDF 소스 분석 시 텍스트(표/단락) 우선 스캔을 원칙으로 하고, 극성/위상 등 텍스트화되지 않는 기호적 정보가 의심될 때에 한해 예외적으로 해당 영역 이미지를 분석하도록 명문화 |
| v1.6 | "원본 데이터 보존 규칙" 절 신설 — 입력된 PDF/문서 원본을 `Original_PDFs` 폴더에 보존하고, 웹 텍스트 3계층(리스트뷰/오버뷰/Full Spec) 입력분은 가공 없이 그대로 `Raw_Web_Data` 폴더의 `[제품명]_Raw_Web_Data.md` 파일로 병합 보존하도록 명문화. 이 규칙은 프로젝트 전용 SKILL.md에만 적용되며, 세션의 시스템 레벨 전역 설정 파일(CLAUDE.md)과는 별개임을 명시 |
| v1.7 | Phase 3-1의 단방향 동기화 규정을 폐기하고 **완벽한 양방향 스키마 동기화(Bidirectional Synchronization)** 규칙으로 대체 — 동종 제품군의 모든 마스터 스키마 파일은 Key 목록과 마크다운 구조가 100% 일치해야 하며, 신규 제품에서 발견된 Key는 기존 제품에, 기존 제품에만 있던 Key는 신규 제품에 양방향으로 `null` 반영해야 함을 명문화. 아울러 "총량/정격 Key 오염 금지" 조항 추가(LA7.16 v1.0의 Total_Power_Capability 오기재 수정 사례에서 도출) |
| v1.8 | "작업 공간 관리 및 버전 체계화 규칙" 절 신설 — (1) 기존 파일 직접 수정/덮어쓰기 금지, 갱신 시 반드시 새 파일 생성, (2) 루트 디렉토리는 카테고리별 최신 파일과 운영 폴더만 유지하고 구버전은 `Archive` 폴더(`Archive_Skill`, `Archive_Data`)로 즉시 이관, (3) 파일명 `_latest` 태그 사용 전면 금지, `[이름]_v[Major].[Minor].md` 형식의 시맨틱 버저닝 채택(Minor: 신규 Key 추가·양방향 동기화·오기입 수정·세부 지침 추가 등 하위 호환 변경, Major: 마스터 스키마 전체 계층 구조 변경 또는 파싱 룰 전면 개편) |
| v1.9 | Phase 3-2(d&b audiotechnik D80 투입) 완료에 따라 "브랜드별 출처 우선순위" 절에 **d&b audiotechnik 기본값 우선순위 확정 사례**를 명문화하고, 무결성 원칙에 "동일 물리량이라도 표기 방식(부호/기준점)이 다르면 임의 변환 금지" 조항과 "확정된 비존재(0)와 미확인(null)을 구분 표기" 조항을 추가. 아울러 신규 브랜드 투입 시 기존 스키마의 Key 개념과 완전히 대응되지 않는 항목이 발견되면 억지로 끼워 맞추지 않고 브랜드 고유 신규 섹션(예: `d80_specific`)을 신설할 수 있음을 "스키마 동적 발견" 원칙에 보강 |
| v1.10 | "작업 공간 관리 및 버전 체계화" 절에 **Surgical Versioning Protocol(수술적 버전업 규칙)** 신설 — 새 버전 파일 생성 시 백지 재작성을 금지하고 Duplicate(복제)→Archive(구버전 이관)→Surgical Edit(대상 부분만 최소 수정) 3단계를 의무화. 세션 중 Write 도구로 대용량 파일을 전체 재작성하다 저장 동기화 지연으로 내용이 잘리는 사고가 재현된 것을 계기로 도입 |
| v1.11 | Phase 4 실전 라운드(LA1.16i/LA2Xi/LA4X/LA7.16i 4개 제품 투입, 누적 8개 제품 도달)에서 확립되었으나 문서에는 미반영이던 관행을 명문화: (1) "작업 공간 관리" 절에 **신규 제품 스켈레톤 재사용 규칙** 신설 — 완전히 새로운 제품을 최초 파싱할 때 아키텍처가 가장 유사한 기존 제품 파일을 구조적 뼈대로 복제해 시작하는 절차를 공식화, (2) 출력 포맷 규칙에 **6. Null Report 섹션** 신설 — 파일 하단에 전체 null Key 목록과 사유(비적용/미확인), 확정적 비존재(0/No) 처리 건수를 요약하는 관행을 표준화, (3) 무결성 원칙의 "확정된 비존재(0)와 미확인(null) 구분" 조항에 **판단 근거의 원본성 요건**을 보강 — 실제 원본 문서를 재스캔하지 않고 기존 파싱 결과물 텍스트만으로 "비존재"를 판단한 경우는 0이 아닌 null로 보수적으로 유지해야 함을 명시(LA12X v2.9 브릿징 Key 판단 사례에서 도출), (4) 용어 매핑 사전에 브릿징 모드(Bridging_Support, BTL/PBTL 출력 Key군)·AES67_Support·Service_Port_Type·Speaker_Terminal_Block_SE_BTL_Wiring·PBTL_Activation_Method 항목 추가, (5) Phase 4 워크플로 서술에서 "현재 누적 제품 수는 N개" 식의 하드코딩된 실시간 카운터를 제거 — 이런 실시간 프로젝트 상태 값은 SKILL.md가 아니라 Session_Transfer.md에서만 추적하도록 문서 역할을 분리(두 문서 간 실제로 괴리가 발생한 것을 계기로 도입) |
| v1.12 | 앰프 외 스피커/앰프렉 등 신규 제품 카테고리 투입을 앞두고 작업 폴더 구조를 **카테고리/브랜드 계층형**으로 재편(`{category}/{brand}/...`)한 것을 반영 — "작업 공간 관리 및 버전 체계화" 절의 "루트 디렉토리 구성 원칙"과 "Archive 폴더" 조항을 갱신: (1) 제품 마스터 스키마, `Original_PDFs`, `Raw_Web_Data`는 더 이상 작업 폴더 루트에 직접 두지 않고 `{category}/{brand}/` 하위(현재 카테고리는 `amplifiers`, 브랜드는 L-Acoustics=`LA`, d&b audiotechnik=`db`)에 둔다, (2) 제품 마스터 스키마 구버전 Archive는 `Archive/Archive_Data/{제품명}`이 아니라 각 브랜드 폴더 내부의 `{category}/{brand}/Archive/{제품명}`으로 이관 — 스킬/세션전달 문서처럼 카테고리에 종속되지 않는 전역 문서(`Archive_Skill`, `Archive_Meta`)만 루트 `Archive/`에 남긴다, (3) 신규 카테고리 투입 시에도 동일한 브랜드 하위 폴더 패턴(제품 스키마 루트 + `Original_PDFs` + `Raw_Web_Data` + `Archive`)을 그대로 재사용하도록 명문화. 스키마 Key 구조나 파싱 룰 자체의 변경은 없으므로 Minor 버전으로 분류 |
| v1.13 | 공식 Agent Skills 작성 베스트 프랙티스(간결성, 시간 종속 정보 지양, 복잡한 워크플로의 체크리스트 패턴) 기준으로 문서 품질 정비: (1) 버전 이력 표를 SKILL.md 본문에서 분리하여 루트 `SKILL_Changelog.md`(append-only 전역 문서)로 이관 — SKILL.md에는 참조 안내만 남겨 스킬 로드 시 컨텍스트 소모를 절감, (2) 서두의 대상 실행 환경 서술을 현행 모델 세대(Sonnet 5 주력) 기준으로 갱신, (3) Surgical Versioning Protocol과 신규 제품 투입 워크플로에 **복사형 체크리스트**를 추가하고, 순서·분기가 있는 절(Surgical Versioning, Phase 진행, 신규 제품 투입)에 mermaid 다이어그램을 도입해 실행 순서를 시각화, (4) "루트 디렉토리 구성 원칙"에 `SKILL_Changelog.md`를 전역 문서로 등재. 파싱 룰 자체의 의미 변경은 없으므로 Minor로 분류 |
